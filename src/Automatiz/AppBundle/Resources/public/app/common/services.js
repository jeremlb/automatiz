define(["angular"], function (angular) {
    var module = angular.module("common.services", []);


    module.service("User", ["$http", function ($http) {
        var service = {
            //me: me,
            //stat:
        };


        return service;
    }]);
});