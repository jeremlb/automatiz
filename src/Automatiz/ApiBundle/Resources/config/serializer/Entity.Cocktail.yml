Automatiz\ApiBundle\Entity\Cocktail:
    exclusion_policy: ALL
    properties:
        id:
            expose: true
            groups: [default, details]
        name:
            expose: true
            groups: [default, details]
        has_alcoohol:
            expose: true
            serialized_name: hasAlcohol
            groups: [default, details]
        createdAt:
            expose: true
            groups: [default, details]
        updatedAt:
            expose: true
            groups: [default, details]
        note:
            expose: true
            groups: [default, details]
        description:
            expose: true
            groups: [default, details]
        steps:
            expose: true
            groups: [details]
