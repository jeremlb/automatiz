FOS\UserBundle\Model\User:
    exclusion_policy: ALL
    properties :
        lastLogin:
            expose: true
            groups: [details_user, user_info, user_detail]
        email:
            expose: true
            groups: [details_user, user_info, user_detail]
        username:
            expose: true
            groups: [cocktail_info, cocktail_detail, user_info, user_detail]
        enabled:
            expose: true
            groups: [details_user, user_info, user_detail]